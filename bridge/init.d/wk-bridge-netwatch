#!/sbin/openrc-run
# A supervised loop rather than a timer: OpenRC has no timer units.
description="Escalating recovery for the uplink and the segment"

supervisor=supervise-daemon
command=/usr/local/sbin/wk-bridge-netwatch
respawn_delay=10
respawn_max=0

depend() {
    need net
    after tailscale
}
