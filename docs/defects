Everything is answered inline.

Focus on these defects:

- zsh alt+arrow keys not wokring from zed terminal

- wk key steup is overly verbose; Also, there should be a one-click link for all keys to generate them (or otherwise it should be done programatically). gh-pat does not fill the repo access correctly, it defaults to all repos.

Making a new workspace on moose:

- wk new on moose still leaves head in detatched state, depsite previous claims this issue was fixed. This needs a real test  so it stops regressing. git fetch origin is very slow after new creation too.

- wk sync says it is rebasing automatically. It shouldn't touch the current branch, since that might lose work. git fetch origin still slow af after a wk sync for some reason. wk sync should do the work to make sure that fetching all four remotes is super fast, then fetch them inside each repo.

- wk sdk should be updated for each new container

All new workspaces (tart and moose):

- on both tart vms and on moose, I get that this command takes forever on a new workspace: git fetch fork; git fetch origin; git fetch wpe; git fetch forkwpe; git pull.

- claude inside a wk container requires authentication still, and skips checks. Checks are too slow and should be done in parallel. Even outside, with a key, wk ai claude still doesn't authenticate.

- new workspaces should have git-webkit setup already (git-webkit setup) using the pat key

- wk rm should remove know_hosts entries

Remember the goal: everything should be reproducable. It should be easy to reset the dev environment to a known good state. Each workflow should be tested, because we keep getting bugs that are fixed on one workflow/driver reproduce on another. Generalize fully each defect. Prefer good architure that makes bugs impossible over fiddly hacks, and use 3rd-party tools / libraries whenever you can. Whenever it is possible to upstream something, call it out.

For another time:

- wk build: crtl-c should cancel build. wk build should not proceed if another build is already running, and there should be a --kill option for detatched builds.

- wk status should always give the status of everything wk can do. In the middle of a build, an A/B task, etc. should all be reported as in-progress with checkboxes next to them including the future next steps the running script will take, instructions to kill the task, and enough info to debug failures in wk-tools or in the task.

- wk push fails open on remote targets.

- rpi has some fixes in ~ that should be incorperated into this repo to set it up from scratch.

- liteLLM keys should be generated from: https://ai.igalia.com/ui/api-keys/. Configure pi.

- In general, commands should parallesize when they can, since wk is very slow.
