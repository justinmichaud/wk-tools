Defects and decisions. Newest decisions at the top; answer inline.

================================================================================
NEEDS YOU
================================================================================

1. buildbox4 has no libc++, so the `-stdlib=libc++` flags you asked for cannot
   go in. Measured: `clang++ -stdlib=libc++` there cannot find <string>, and
   `gtk-release` builds fine against the system libstdc++ (it did, in 3m00s,
   while this was being written). The generic half of your flag list is now the
   default for every CMake config (build/configs.sh, _CFG_DEFAULT_ARGS /
   _CFG_DEFAULT_CMAKE); the libc++ half is not applied anywhere, and
   targets/hosts/buildbox4.conf records why.

   To have it: `sudo apt-get install -y libc++-19-dev libc++abi-19-dev` on
   buildbox4 (theirs to run, or ask), then say so and it goes in the conf.
   Or was that flag list copied from a different machine? [decision]

2. The new default flags are verified as far as `cmake` -- the whole set
   configures cleanly on buildbox4 for GTK with clang 19, in a scratch build
   directory that was then removed. A compile-and-link verification is owed and
   was not started: buildbox4's load average is 142, `wk build` derives 1 job
   from that, and `-DUSE_LIBBACKTRACE=OFF` (previously ON there) invalidates
   the whole tree -- a full rebuild at -j1 in your workspace. Say when the box
   is quiet, or when to just do it anyway. [decision]

3. "Never rsync wk tools, always use git" is decided (git push, refuse when
   dirty) and NOT yet implemented. Before it lands, one consequence to confirm:
   there are three rsyncs of the tree, not one --

       targets/remote.sh   -> a build box      ($ROOT/tools)
       targets/vm.sh       -> a tart guest
       host/macos/vmtools.sh -> the podman VM  (/opt/wk-tools)

   The third is what every container workspace mounts and what `wk pr`, `wk
   build` and friends run when they forward into the VM. Refusing to sync a
   dirty tree there means no container command can be tested against an
   uncommitted wk-tools edit at all -- and this agent cannot commit. Does the
   rule cover the podman VM too, or only the machines across ssh? [decision]

4. G, still open from the last run: moose's ~/Development/wk-tools has
   uncommitted edits (bench/mac-ab-summary.sh, cmd/bench, cmd/pi, cmd/status,
   lib/bench.sh, lib/common.sh, lib/wkdata.py) that are not in this tree.
   Nothing here has touched them and `wk sync --tools` has not been run. Yours
   to commit or discard. [decision]

5. `MACH_` is being renamed (it collides with macOS's own "mach"). Proposed
   `NODE_`: boot/machines/<name>.conf becomes NODE_SSH, NODE_DEVICE, NODE_LOCAL,
   and machine_load/machine_list keep their names. It is one sed over ~700
   occurrences in 35 files -- and it is being held for two reasons, not one: the
   word is yours to pick, and a rename that size would collide with moose's
   uncommitted edits to cmd/status, cmd/pi and lib/common.sh (item 4). Resolve 4
   first, then say the word. [decision]

6. The tart guest password becomes `1` as asked, which means provisioning
   actually changes the account password (the Cirrus images ship admin/admin
   and `sysadminctl` needs the current one). Confirm the guest account is
   throwaway in the sense that matters: nothing on the tailnet trusts it, and
   `wk rm` destroys it. [decision -- proceeding on yes unless you say otherwise]

7. jsc-debug lost `-DUSE_LIBBACKTRACE=ON` in commit 027607f, which was not
   made from here, while its own one-line description still said
   "libbacktrace" -- so the config and the listing disagreed. The listing now
   matches the code (jsc-debug: "JSCOnly, Debug, assertions on"), on the rule
   that the machine wins. gtk-debug still asks for libbacktrace. If jsc-debug
   is meant to have it, putting the flag back is one word; if the removal was
   because the library is not on every machine, then gtk-debug's copy should go
   the same way. Which? [decision]

8. `wk key tailnet` now refuses an API access token and an OAuth client secret
   by name (both start `tskey-`, both administer the whole tailnet, and this
   key is copied onto every card written from here). What it cannot check from
   a key -- tagged, reusable, not ephemeral, expiry -- it reports as unverified
   with the console link, because answering those needs an API token, which is
   the credential it just refused to hold. Nothing further is checkable without
   holding one. Good enough, or do you want a read-only API token stored for
   it? [decision]

9. Your guest `mya` is stopped, and I stopped it. Measuring the desktop
   question above needed running vm/desktop.sh against a live guest, and its
   first version killed Setup Assistant -- which on macOS *is* the login-time
   agent, not a window on top of a session, so the guest fell back to the login
   window. Clearing the screen lock from there needs a logged-in session
   (`sysadminctl` answers `MKBDeviceSetGracePeriod error -8` without one), so
   the way back is a restart -- and `wk vm start mya` now refuses, correctly,
   because the podman machine holds the whole memory envelope:

       podman machine stop wk && wk vm start mya

   The corrected script kills nothing, and `wk vm check` names a restart as the
   remedy for a panel already on screen. Nothing in the guest was lost; it is a
   clone. [needs you: one command, or `wk rm mya` if it was a scratch guest]

10. Another session was editing this tree at the same time as this run --
    build/configs.sh, build/build-in-target.sh, cmd/gui, cmd/run, cmd/profile,
    cmd/bench, adding CFG_SCRIPT/WK_BUILD_SCRIPT for `build-jsc`. Two of those
    files are ones this run also changed (build/configs.sh above all), and one
    full `wk selftest` came back with 13 build-test failures purely from files
    moving underneath it -- the same suite passes now. Nothing here was
    reverted, and the two sets of edits coexist in the file, but this is exactly
    the hazard CLAUDE.md names: if that work is still in flight, re-run
    `wk selftest --quick` before believing any earlier failure count.

================================================================================
DONE THIS RUN
================================================================================

- A workspace starts already authenticated, on every target. One token per
  machine: `claude setup-token`, then `wk key claude` stores it at
  $WK_STORE/secrets/claude-token (0600, taken on stdin so it is never in `ps`,
  never printed anywhere). shell/bashrc is the only reader, exporting
  CLAUDE_CODE_OAUTH_TOKEN, so a person typing `claude` in an editor pane gets
  it and not only `wk ai claude`. Each target hands it over the way it already
  hands over the proxy:
      container   a symlink onto the read-only /secrets mount, made whether or
                  not a token exists yet -- so storing one later reaches every
                  container with no rebuild, and withdrawing one reaches them
                  the same way
      macOS guest a copy written by the host on every start, removed again when
                  the store has no token
      build box   a copy written by `wk remote setup`, removed the same way --
                  it is a machine other people are on, so a token left behind
                  after it was withdrawn here is the state that would matter
  `wk doctor` reports it and README's setup lists it. tests/test_agent_token.py
  (18, no real token anywhere in them).

  Caught while wiring it, and worth knowing: the token resolves against
  $WK_STORE_DEFAULT, not $WK_STORE. targets/vm.sh points $WK_STORE at its own
  state directory, so `wk vm start` would have looked in a directory `wk key
  claude` never writes to and found nothing, silently. Two tests hold it.

- The old shared credentials file is gone with it (container/firstrun.sh,
  cmd/doctor): it was Linux-only -- Darwin keeps those in a login Keychain that
  an ssh session or an editor's remote server never unlocks -- and it needed a
  `claude login` from inside a workspace to exist at all, so it could never be
  the answer for a guest or a build box. Two ways to authenticate is one too
  many. Nothing was stranded: $WK_STORE/secrets was empty here. Say the word if
  you would rather keep it; it is a few lines back.

- The egress allowlist covers ordinary development (container/proxy/wk-proxy.py):
  npm, Homebrew, crates/rustup, Apple's developer and software-update hosts
  (so `xcode-select --install` and Xcode's first launch complete), and
  certificate validation -- valid/ocsp/crl.apple.com and i.pki.goog, without
  which Gatekeeper will not launch anything downloaded. Apple is named host by
  host, never by suffix: the refusals a guest produces are mostly iCloud, Siri,
  Spotlight, ads and news, and none of that is development. Declared for the
  sandbox audit in the file, BLOCKED_NETS unchanged. tests/test_egress.py.
  Verified from a guest: npmjs/brew/developer.apple/crates/rustup all answer,
  gsa.apple.com still refused.

- A guest's egress reaches every shell, not login shells alone. ~/.wk-egress is
  written by the host on every start (_set_guest_egress, targets/vm.sh, which
  now writes both halves -- the system proxy and the shell variables) and
  sourced by all four rc files (vm/shell-rc.sh). vm/provision-base.sh writes
  nothing about the proxy any more: the address is the host's own on the guest
  bridge and changes, so no image may carry a copy, and a guest that baked one
  in has it stripped on the next start. Deliberately NOT in shell/bashrc --
  that file lives in the guest's own copy of wk-tools, which only a build
  refreshes, and a guest must not be without egress until someone builds in it.
  tests/test_egress.py. Verified in the guest: an editor pane fetched
  https://webkit.org/ (200) where it previously reached nothing.

- `wk vm start` against an already-running guest restarts the host egress proxy
  if it is not listening. It is the guest's only route out, so a proxy that
  died left the guest with no egress and no way back short of a reboot; every
  other fact that branch converges was already written on every start.

- A guest's clock is set from this host on every start (targets/vm.sh,
  _set_guest_clock), and before the base is provisioned -- where a stale clock
  would fail provisioning's first HTTPS clone and then seal that date into
  every clone made from it. Idempotent by measurement: a guest within
  WK_VM_CLOCK_SKEW (30s) costs one ssh and no sudo. It runs before
  _set_guest_proxy, because everything reached through the proxy speaks TLS.
  tests/test_vm_clock.py drives the guest half against a fake date and sudo.
  `wk vm start mya` reported `mya's clock was 958989s out; set from this host`.

- Home/End in zsh (B). Measured cause: shell/bashrc bound the keys and *then*
  ran `bindkey -e`, which links a fresh emacs keymap over `main` and discards
  whatever was bound into the previous one. Machine-dependent, which is why it
  worked on moose: zsh starts in viins when $EDITOR contains "vi", so a host
  without helix lost the bindings and a host with it kept them. The keymap is
  now chosen above the bindings, and ^[OH / ^[OF (application cursor mode,
  which zle's own smkx asks the terminal for) are bound as well.
  tests/test_shell_keys.py.

- `wk sysimage ls` carries a STATE column: building | ready | none, from the
  workspace's own live answer. An image being built is the one most likely
  being looked for and `-` in the size column was not an answer.
  tests/test_card_edits.py.

- Default CMake flags for every config, and per-machine flags per config
  (`WK_TARGET_CMAKE_<config>`, `WK_BUILD_ARGS_<config>` in a machine's conf).
  Each config now states only what differs from the default. tests/test_build.py.

- `wk pr <ref>` no longer refuses a pull request as a missing workspace. The
  declaration gained `takes=<n>`: how many positionals belong to the command
  after the name, so a lone one is the command's. An audit test holds every
  command with an optional name to it.

- On a shared build box the *directory* now names the workspace: `cd
  ~/wk/ws/<name> && wk build gtk-release` works, as it does inside a container
  workspace. There is no workspace marker on those machines -- several live
  side by side under one root -- which is why the in-workspace interface only
  ever worked in a container. tests/test_dispatcher.py.

- git identity and speed settings reach every machine, from the one file that
  declares them (dotfiles/gitconfig) rather than a copy per machine:
  container/firstrun.sh and remote/provision.sh include it, `./setup` removes a
  [user] section that shadows it (git takes a key's *last* value, so an include
  loses to anything written after it), and `wk doctor` checks all three.
  Found by it: devbox-arm64-2 has `user.name = no` and `user.email = no`, so
  every commit made there is authored by "no <no>"; buildbox4 has no identity
  at all, so a commit there is refused after the work. Both fixed by re-running
  `wk remote setup <target>`.

- `wk doctor --all` asks every build machine what it has, read-only over ssh:
  the tools a build needs, the identity a commit there would carry, and any
  build variable that machine's own profile sets. One list of prerequisites
  (remote/deps.sh) answers for `wk remote setup`, provisioning and doctor, and
  whatever is missing comes back as one exact root command -- provisioning
  still never takes root. tests/test_remote_deps.py.
  Found by it: buildbox4 has no ccache, so every build there starts cold.

- A machine's own environment is no longer overwritten: shell/bashrc keeps a
  pre-set $EDITOR/$VISUAL, and `wk doctor --all` prints any CC/CXX/CCACHE_DIR
  a build box's profile exports, saying plainly that wk's build sets its own
  and ignores it.

- The approved macOS settings from J are in host/macos/defaults.conf
  (AppleWindowTabbingMode, dock show-recents/mru-spaces, the two hot corners,
  screensaver askForPassword, Safari's Develop menu). The Dock is restarted
  when one of its keys changes -- it reads its preferences once, at launch, so
  a written key nothing restarts is a setting that is in the file and not on
  the screen. The Accessibility key-repeat values are left out, as you said.

- `wk key tailnet` and admin/wk-card-priv both refuse anything but
  `tskey-auth-`. tests/test_key.py.

- Zed opens every project trusted already (`session.trust_all_projects`,
  dotfiles/zed/settings.json) -- both defect entries asking for it are the
  same one, and it was already true.

- `wk <cmd> -h` prints a closed set of values by running the one list that
  defines it: the declaration gained `values=<flag>`, and `wk build -h`, `wk
  boot -h` and `wk sysimage -h` now end with today's configs, fleet machines
  and image profiles. Nothing is copied into a header, so nothing can go stale.
  `wk sysimage build --list` becomes `wk sysimage --list` -- one spelling.
  tests/test_help_values.py.

- The lifecycle notes in README.md are four numbered steps, and the third one
  is stated as what it is: a hand moving a card from the reader to the board.
  The example no longer writes a card for the rpi3 and then arms the rpi4.

- `wk claude` is now `wk ai claude`, with `wk claude` a tombstone naming its
  replacement. One command for every coding agent: the agent is the first word,
  everything around it (which workspace, the sandbox check, the commit wall,
  the push switch, remote control) is shared, and `wk ai pi` refuses by name
  with what it still needs rather than pretending to exist.

- Claude remote control is on by default: `wk new` starts it in the workspace
  it just made and `wk start` starts it in every container it brings back up,
  both through `wk ai claude --rc` and neither able to fail because of it.
  WK_NO_CLAUDE_RC=1 turns it off -- one variable, since both call the same
  command. The wiring is tested (tests/test_claude_rc.py); a live check needs a
  container and a Claude CLI in it, and is owed.

- A macOS guest lands on an empty desktop, and something now *checks*: `wk vm
  check <name>` (read-only) reports the console session, the screen lock, the
  screen saver, display sleep, Setup Assistant's panes, the Software Update
  offer, and whatever is modal on screen right now -- each with its remedy.
  vm/desktop-probe.sh reads, vm/desktop.sh writes, and nothing else does either.

  Two root causes, both measured on the live guest rather than reasoned about:
    * `defaults -currentHost` writes per *hardware UUID*, and `tart clone` gives
      the clone a new one -- so every ByHost setting the golden base holds (the
      screen saver among them) does not apply to any guest cloned from it. Those
      are now written on every `wk vm start`, by the same one script the base
      runs. `wk vm check mya` reported the saver armed; after the fix, off.
    * Setup Assistant's `DidSee*` list had grown: two panes on this macOS were
      unset, and one was on the desktop of a running guest.
  The Software Update offer (a modal panel on the desktop) is turned off too.

- The guest gets the same shell rc as every other machine, from .zshrc as well
  as the profiles -- which is the actual reason `wk zed mya` opened a terminal
  with no `wk` on PATH: a profile is read by a *login* shell and an editor's
  terminal pane is not one, so PATH written only into .zprofile was missing in
  the one place a person types.

- The guest's login is one fact in one place (targets/vm.sh: WK_VM_USER,
  WK_VM_PASSWORD=1, WK_VM_IMAGE_PASSWORD=admin -- the image's, used once to
  change it), printed by vm_login_note at each of the four moments a person is
  about to look at the guest's window: provisioning it, starting it, entering
  it, opening Zed on it. The screen lock is left alone unless the password authenticates
  first: `sysadminctl -screenLock` takes the account's current password, and
  offering it a wrong one was observed to leave the lock armed.

- tests/test_dispatcher.py's top-level-call-order audit was blind to a
  function whose opening brace carries an argument comment (`f() { # <arg>`),
  this tree's house style -- so it reported two false positives in cmd/pi and
  would have missed a real one anywhere else.

================================================================================
OWED, WITH THE DECISION ALREADY MADE
================================================================================

- Never rsync wk-tools: git push, refuse when dirty (decision 3 above narrows
  what it covers). All three rsyncs, and the remote copy becomes a real
  checkout -- which also gives `wk status` a way to say "the tools over there
  are old" instead of printing `?`. Measured: buildbox4's copy has no
  `ws_upstream_line` at all, which is exactly why `wk status image-decoders`
  reports BASE=`?` -- the column works, the tooling over there is months old.

- The pi agent itself (docs/Urgent/pi.md): the litellm key as `wk key set
  litellm` into the store's read-only-mounted secrets/, a `wk doctor` line, and
  registry.npmjs.org added to the proxy allowlist for `npm install -g`.
  `wk ai pi` refuses by name today, saying what it needs.

- Battery (L): `charge_control_end_threshold` set by `wk bridge setup` on both
  phones and read by `wk doctor --all`; on macOS the honest line, "plugged in,
  N% -- no OS limit exists".

- `wk rm br-rescue2` (or `podman rm`) -- the Exited container on this Mac's
  host podman connection. You said to go ahead; not done yet.

================================================================================
STILL OPEN
================================================================================

- Live check owed: claude remote control actually coming up in a container that
  `wk new` just made, and in one `wk start` brought back. Neither is exercisable
  from a test (both need a real container with a Claude CLI in it).

- `wk status` should also say whether the container SDK image is up to date,
  and BASE should stop reading `?` (see the tools-sync item above).

- Confirm layout tests can run headless and with a real GPU.

- Confirm ccache still caches across a change of -j. (Nothing in the flags wk
  sets varies with the job count -- CCACHE_* is fixed and -j reaches ninja, not
  the compiler line -- but that is an argument, not a measurement.)

- Help lists every valid value for the three sets that live in one list
  (configs, machines, profiles). Still owed: the sets that live in prose --
  `wk bench <plan>`, `wk profile`'s profiler names, `wk pi`'s subverbs.

- `wk new` should fetch by default; `wk fetch` should be faster; a plain `wk
  new` leaves git detached with several remotes. You approved one `wk sync` +
  `wk new` here or one `wk new` on moose to measure it (F).

- Finish the audit of the shape `wk zed <workspace>` had -- an argument a
  command re-decides for itself and then mishandles. The workspace name is the
  dispatcher's alone now, and `takes=` closed the case where a command's own
  positional was read as one; configs, flags, paths and subverbs are still
  parsed command by command.

- HANDOFF-wk-cli's remaining [needs a test] items: four of six are still owed
  (tests/test_owed_cli_color.py and tests/test_owed_cli_exit_code.py landed).

- start zed in trusted mode

- tart password is still set as admin, not 1.

- wk * commands still ask for workspace name inside tart vm. It seems like the abstraction to check if you are in a workspace or not is not working correctly.
