Your scope is everything. Keep going until every defect and handoff item is done. You will be running unattended soon, so prioritize user intervention first. Surface decisions that need to be made here in this file at the top.

Use cheaper subagents with smaller models wherever possible.

Prompt for the next agent (written 2026-08-27 00:30; delete when read):

  You are continuing an unattended cleanup of wk-tools. Read CLAUDE.md, then
  README.md, then this file top to bottom. The maintainer committed the
  tree as d8f05e2 at 23:19 (detached HEAD; local `main` still points at the
  pre-rebase 3cc0aa6 -- `git checkout -B main HEAD` reattaches it, the
  permission classifier refused that here); everything after it is
  uncommitted work from this session. `./wk selftest` passed in full on it:
  380 tests, OK, 8 skipped (00:20); `--quick` on the final tree at 01:05:
  422 tests, OK, 17 skipped. `./wk selftest --quick` now runs every module
  with WK_TEST_QUICK=1 so hardware-gated tests skip by name (it used to be
  two modules by filename pattern, which is why it kept saying "88").
  First: `./setup --stage vmtools` (the podman VM copy is what a forwarded
  container command runs; sync it after any edit), then `./wk selftest
  --quick`. Then, in order:
  1. The "Decisions needed" list: anything answered inline, act on; the rest
     stays blocked. Decisions 1-3 gate every card write; 5, 6 and 8 are moot.
  2. moose: three yocto images (rpi3 2.52, rpi4 2.52, rpi5 2.54) and the
     first buildroot image (wpewebkit-2.38 rpi3, sdcard.img 1.6 GB) are
     built for real -- `ssh moose 'wk sysimage ls'` lists them. Nothing is
     building there now.
  3. Owed from this session, each with a test that must run: the comment
     pass on the files still above 40% (listed under the comment defect
     below); `wk sysimage write --from` of a yocto/buildroot image edits a
     local scratch copy and needs mtools/debugfs/sfdisk where it runs (a
     card-side verb in admin/wk-card-priv is the design, docs/HANDOFF-
     fleet.md); helix/lazygit on remote and VM targets (docs/Nice to
     have/HANDOFF-helix.md); the option audit on status, bench, pr, run, gc,
     pi, pick, session (--json/--count/unknown-flag refusals); the
     HANDOFF-test-runner.md list of tests to write (sync scope selection,
     prompts-guard-destructive audit, the 71 WK_* overrides).
  4. Then the HANDOFF-*.md work lists. Rules: sonnet agents on disjoint
     files; never two on one file; an agent never runs `git checkout`,
     `git restore` or `git stash` (one did, and reverted a fix); never run
     `wk selftest` while an agent edits; never `wk sync --machine`, `wk
     quiesce on`, `wk stop`, or a card write unprompted. Every "done" needs
     a test that ran, and every agent report is checked against `git diff`.

- Audit every command for breakage of the shape `wk zed <workspace>` had: an
  argument the command re-decides for itself and then mishandles. The workspace
  name is now the dispatcher's alone; the other arguments -- configs, flags,
  paths, subverbs -- are still parsed command by command.

Decisions needed (from the agent; answer inline, or edit the item below):

1. rpi3 is online on the tailnet as `rpi3` right now. Writing its new recovery
   card is refused by the rule you asked for (a node named `rpi3` exists; the
   new join would be `rpi3-1`). To write it, remove the `rpi3` node in the
   Tailscale admin console first -- the running rpi3 is then unreachable by
   name until the new card boots. Same will apply to rpi5 when its card is
   written. rpi4 is not on the tailnet, so its write is not blocked. Say "go"
   and remove the node, or say to keep the old node and skip rpi3 for now.
   (agent 2026-08-26 late: still blocked -- `rpi3` is on the tailnet; nothing
   was removed. All three yocto images are built for real on moose, with
   tailscale 1.102.2 + wk-wifi-join + wpa-supplicant in each manifest:
   rpi3 2.52 19:08, rpi4 2.52 18:28, rpi5 2.54 19:27 local. Ready to write.)
2. WiFi credentials for the Pi cards: none exist yet. The file is
   `~/.config/wk/wifi` on the machine that runs `wk sysimage write` (this
   Mac), two lines: `ssid=<name>` and `psk=<passphrase>`; never in git. Create
   it, or say which machine should hold it.
   (agent: still blocked -- ~/.config/wk/wifi does not exist on this Mac.)
3. "rpi5 2.54 yocto image": no 2.54 config exists; upstream has
   `webkitglib/2.54`. I am adding `image/configs/webkit-2.54-yocto-rpi5-64.conf`
   as a copy of the 2.52 one with the branch changed. Say if you meant WPE
   (`wpewebkit-2.54-...`) instead.
4. `wk claude --rc`: read as Claude Code Remote Control and built that way
   (`wk claude <ws> --rc` starts the `claude remote-control` server in the
   workspace, `--rc --stop` ends it; the first start runs one interactive
   session because the trust dialog has no headless path). Say if you meant
   something else.
5. Git: origin/main is one commit ahead (5905f22, 17:09, from moose) and it
   rewrites `lib/target.sh` -- a `ws_on_target` probe that keeps the registry
   as the fast path. This working tree carries the agent's uncommitted removal
   of that registry (`ws_target`/`ws_exists` probe the stores only; `wk gc`
   deletes the old dir), which is the rule you set. A `git pull` here will
   conflict in lib/target.sh. Recommendation: keep the registry-free version
   and fold your `t_info` probe into `ws_exists` (it already has one for the
   container case). Say "reconcile" and I do it (stash, pull, resolve, re-run
   the suite); or you do it.
   (agent: moot -- the tree was rebased onto origin/main ecb66ed as aa2e8c0,
   registry-free, and the full suite runs on it. HEAD is detached at aa2e8c0
   and the local `main` ref still points at the pre-rebase 3cc0aa6, whose
   content is all in aa2e8c0; `git checkout -B main aa2e8c0` reattaches it --
   left for you, the permission classifier refused it here.)
6. The rpi3/rpi4 "recovery image builds" on moose finished in 2 minutes and
   produced NO new image: bitbake did not run, and the wrapper reported the
   day-old image as built (no WiFi layer in it). Being fixed (image stage
   must run bitbake and prove the artifact is newer than the run). No decision
   needed; noted so the fast "done" is not believed.
   (agent: fixed and verified -- each yocto-image.log ends with a bitbake
   "Tasks Summary ... all succeeded" and the wic.xz postdates the run.)

7. A "transient, 100% reproducible perf image using a normal distro" for
   rpi5/moose (your HUMAN-benchmark-start note) does not exist: the fleet
   boots yocto images, and `image/profiles.sh` tombstones `perf-linux-*` to
   them. Is the distro perf image still wanted (it is a new builder,
   `IMG_BUILDER=distro`, L)? Or is the yocto image the perf image now?

8. The yocto import keeps rootfs.tar.xz (~600 MB per system) and nothing
   reads it (docs/HANDOFF-yocto.md). Keep it as an archival artifact, or stop
   importing it?
   (agent: moot -- there is no import any more: the store is gone and a build's
   output stays in the workspace that built it; the builder still writes its
   own rootfs.tar.xz there and nothing copies it.)
9. Pre-field images have no IMG_ROLE, so `wk sysimage ls` prints `unrecorded`
   for them (docs/HANDOFF-boot.md). Keep that word, or say what it should say.
10. `--sysroot` cross-compile (docs/Nice to have/HANDOFF-cross-compile.md) is
    reserved and refused, unbuilt; `wk sysimage build --stage webkit` already
    covers per-board cross builds. Build it (only no-SDK targets such as
    riscv64 would need it), or make the refusal permanent and delete the doc?
11. docs/HANDOFF-reprovision.md is the list for wiping moose and tolken to
    prove reproducibility, with no date. Say "go" to schedule the wipe, or
    leave it undated.
12. Tailscale ACL policy needs an admin-console session: Karen limited to
    immich/nextcloud/overleaf/proxmox with those apps isolated from each
    other; your two accounts full access; a `tag:wk` grant for the Pi bench
    systems that does not reuse `tag:server`; a stance on untrusted-router /
    public-WiFi containment (docs/Security/HANDOFF-tailscale.md 1-4,6,8;
    docs/HANDOFF-linux-pi.md 4). When can you do that session?
13. `wk profile --browser` / `wk gui --browser` on the CMake ports: "the
    browser" = the UI process only, or including its web process? (Mirrors
    the `--lldb ui`/`--lldb web` split.) docs/Urgent/HANDOFF-linux-minibrower.md,
    docs/Urgent/HANDOFF-profile.md.
14. The Mac bench volume is the sticky default startup disk after install, so
    HOST mode is what needs the manual startup-manager click today. Keep that
    (a run needs no manual step), or make host the default and arm each run?
15. Software-update scanning on the Mac bench install is detected and flagged,
    never prevented; the only remaining fix is denying Apple's update endpoints
    in that volume's /etc/hosts (docs/HANDOFF-mac-perf-mode.md). Do it, or is
    flag-and-report enough?

16. Debug symbols for crash triage (docs/Urgent/HANDOFF-debug.md) need
    `ddebs.ubuntu.com` reachable from a workspace, i.e. one more host in the
    proxy allowlist (container/proxy/wk-proxy.py ALLOWED_HOSTS). Widen it for
    that one host, or keep the allowlist as is and fetch symbols on the host?

Everything else below proceeds without you.

Defects:

- Confirm new recovery images for rpi3 and rpi4 build and write fine. Both sd card (rpi3) and second usb stick (rpi4) are connected to rpi5, so no intervention is needed. I give you permission to do this.
  (agent: both images are built for real on moose -- see decision 1. The
  writes wait on decisions 1 and 2. The first-ever buildroot image,
  wpewebkit-2.38 rpi3, finished on moose at 20:08: sdcard.img 1.6 GB,
  `stage 'image' done` after the freshness check. `wk sysimage ls` prints `?`
  in its machine column -- the profile is not derived from a buildroot
  workspace path; owed with the image-store removal.)

- Confirm rpi5 2.54 yocto image works and can be deployed.
  (agent: built for real on moose, 19:27; the write waits on decisions 1-3)

- wk status should load local data quickly first and fill the rest later.
  (agent: the streaming render is in; a bare `wk status` also hung forever --
  a job returning non-zero left no `.rc` marker -- fixed in lib/par.sh,
  tests/test_par.py; 10.5s end to end now)

- Audit every subcommand for option consistency: a missing --quiet, --target,
  --all or --force where a sibling command has one; a parameter silently
  ignored instead of refused; one section of a command getting a feature a
  neighbouring section does not. Known leftovers from the 2026-08-26 pass:
  --quiet is parsed locally by ~11 commands instead of once in the
  dispatcher, the way --force is; wk ls and wk find have no --json, though
  wk status does; --all means "every machine" in wk sync and "every item"
  elsewhere -- one meaning, or two words; --count appears in bench, pick, pr,
  pi, status and sync with no confirmed single semantics.
  (agent: measured -- only cmd/sudo parses a wk --quiet; the other hits are
  git/systemctl flags. --count is a wk flag only in bench and pi; the rest are
  `git rev-list --count`. Being moved into the dispatcher / audited.)
  (agent: done -- --quiet is stripped once by the dispatcher into WK_QUIET and
  honoured by log/info in lib/common.sh; `wk ls --json`, `wk find --json`;
  one meaning of --all documented in `wk help`; unknown flags refused in
  sudo/ls/find/sync/push/doctor/remote/logs/verify; tests/test_options.py.
  Second pass: status, bench, pr, gc, quiesce, test, gui, profile refuse
  unknown flags and extra positionals with `usage:`; cmd/gc parsed only $1
  before. run/enter/claude pass through by documented contract. --count is
  "iterations per run" in bench and pi. Owed: none.)

- High-level docs: an architecture overview and every supported workflow by
  example, very short. README.md now has a first version of this; the item
  stays until it is accepted as done.

- Mass deletion of low-level comments and docs that serve no value. Comment
  density is still ~37% of shell lines; lib/target.sh is 43%.
  (agent: 42% -> 32% of shell lines across the tree, code verified unchanged
  file by file against HEAD; lib/target.sh 51% -> 37%, cmd/status 27%,
  cmd/bench 33%, cmd/* else 27-34%, targets/ 26-30%, bench/ 25-29%, lib/
  <30%, image/boot 27-30%. Still above 40%, all provisioning/board scripts
  one agent judged load-bearing: remote/provision.sh, vm/provision-base.sh,
  container/firstrun.sh, shell/bashrc, image/buildroot-build.sh,
  boot/mac-guest.sh, host/dotfiles.sh, host/macos/sharing.sh, bridge/bin/*,
  the two yocto join scripts. A second opinion on those is owed.)

- docs/Urgent/HUMAN-benchmark-start.md and docs/Urgent/HUMAN-Benchmarking variance.md
  (agent: done so far -- one result schema with a configuration tuple, `wk
  bench report` HTML/histograms/score+time/variance-by-configuration, the
  noise knobs (ASLR, env pad, path pad, shared cache) in every lane; the
  image/bench-mode unification is planned in docs/HANDOFF-bench-unify.md.
  Still owed from the notes: Mac bench-mode checks in `wk doctor` (screen
  state in quiesce, SIP on host / off on bench volume, single manual step),
  the 8-hour autonomous run driver, one run per side under samply with
  jitdump, and the VM rehearsal of the whole flow.)

- Then go through all the docs not in a folder
  (agent: every HANDOFF-*.md touched by landed work lists owed items only
  now -- wk-cli, test-runner, fleet, pi-deploy, memory, debug, helix, clangd,
  yocto, boot -- and the narrating lines ("no longer", "now that", "used
  to") are rewritten present-tense. Not yet re-read end to end: bmc, ab-bench,
  sdcard, linux-pi, linux-arm32, router, reprovision, sandboxing,
  settings-audit, architecture-review, original-helpers, bench-python,
  bench-unify, mac-perf-mode, Security/tailscale, Nice to have/*.)
