Your scope is everything. Keep going until every defect and handoff item is done. You will be running unattended soon, so prioritize user intervention first. Surface decisions that need to be made here in this file at the top.

Use cheaper subagents with smaller models wherever possible.

Prompt for the next agent (written 2026-08-27 late; delete when read):

  You are continuing an unattended cleanup of wk-tools. Read CLAUDE.md, then
  README.md, then this file top to bottom. Everything is uncommitted work in
  the tree (HEAD b133c17); the maintainer commits. The last full
  `./wk selftest --quick` result is the line at the end of this prompt.
  First: `./setup --stage vmtools`, then `./wk selftest --quick`. Then:
  1. Decisions A and B below are the maintainer's; moose is off the tailnet
     (since the morning of 2026-08-27; its BMC bridge phone is off too), so
     everything under "waits on A" -- card writes for rpi3/rpi4/rpi5, the
     rpi5 2.52 rebuild, deleting the pre-field images, timing a second yocto
     build -- is blocked until it answers `ssh moose`. Check with
     `tailscale status | grep moose` before planning around it.
  2. Land the leftovers of this session, each with a test that ran:
     - admin/install.sh installs boot/check-boot-files.py to
       /usr/local/libexec/wk-check-boot-files.py (admin/wk-card-priv's
       `boot-check` verb refuses until it does; TODO in the helper).
     - boot/disk.sh disk_write_stream still has the zstd-or-raw fallback and
       disk_eject skips silently without udisksctl: stream the source's own
       compression and decompress on the card machine by extension
       (`_from_filter`, cmd/sysimage), refusing when the decompressor is
       absent; report a missing udisksctl (udisks2 is now in apt.txt).
     - grep mtools/mcopy/debugfs: if nothing uses them any more, drop mtools
       from host/linux/apt.txt and its comment; boot/pi-usb.sh:17 still says
       the kernel check happens "before writing" (it is after, on the card).
     - WK_* audit leftovers: cmd/find, cmd/status, cmd/sync,
       host/macos/vmtools.sh, image/pmos.sh read WK_SSH_TIMEOUT inline --
       switch to wk_ssh_timeout() (lib/common.sh); one policy for
       WK_STALL_SECONDS/WK_ABORT_SECONDS (lib/watchdog.sh 300/1800, cmd/bench
       900/5400, image/yocto.sh 900) -- the bench/yocto raises are deliberate,
       state that once where the default lives; targets/remote.sh header
       lacks WK_TARGET_WPE; image/yocto.sh's `${WK_TARGET_KIND:-container}`
       default is unreachable (lib/target.sh always exports it) -- delete.
     - facts displaced from the handoffs, to place as one comment line:
       "never turn the radio off during a run" at cmd/quiesce `on)`; "never
       trim the first Speedometer iteration" at bench/mac-ab.sh's
       --count/--rounds parsing; "no overclock in the EEPROM --
       SDRAM_BANKLOW/BOOT_ORDER are shared by both modes" at cmd/pi
       cmd_boot_order.
     - docs/HANDOFF-sdcard.md's last item (image edits onto the card) is done
       -- delete it; docs/HANDOFF-test-runner.md still names install_units /
       image_check_root.
     - tests/test_wifi_seed.py's netplan-parser tests skip on macOS (no
       PyYAML); they ran once for real in the podman VM. A Linux
       `wk selftest` runs them.
  3. docs/HANDOFF-host-tools.md (new, from the maintainer's request this
     session) and the rest of docs/HANDOFF-*.md, now owed-only lists with a
     [bracket] naming what each item waits on; do the unbracketed ones first.
  Rules: sonnet agents on disjoint files, never two on one file; an agent
  never runs `git checkout`, `git restore` or `git stash`; never run
  `wk selftest` while an agent edits; never `wk sync --machine`, `wk quiesce
  on`, `wk stop`, or a card write unprompted; every "done" needs a test that
  ran, every agent report is checked against `git diff`. When you message an
  agent, address the one that owns the file (one message this session went
  to the wrong agent and the work was not done).
  Evidence: `./wk selftest --quick` on the final tree (2026-08-27 late):
  645 tests, 1 failure fixed in place (tests/test_prompts.py's raw-read
  audit now lists cmd/sysimage's file read), 22 skipped; tests.test_prompts
  re-run OK after the fix. The last all-green full run was 501 tests OK
  before round 2 (the WK_* audit, the card-side verbs) landed -- re-run
  the whole suite first.


- Audit every command for breakage of the shape `wk zed <workspace>` had: an
  argument the command re-decides for itself and then mishandles. The workspace
  name is now the dispatcher's alone; the other arguments -- configs, flags,
  paths, subverbs -- are still parsed command by command.

Decisions needed (from the agent; answer inline, or edit the item below):

A. moose is off the tailnet (last seen this morning; its BMC bridge phone
   librem5-oob has been offline 5 days), so nothing here can reach it. Every
   image step waits on it: the built images live there, moose is the machine
   that drives a card write onto rpi5's reader, the pre-field images to
   delete (decision 9) are there, and the yocto incremental-build proof
   (decision 6) is a rebuild there. Power it on, or say when it will be.

You did something to kill it, you are running on moose!

B. rpi4 carries two wk-written media (rescue USB, bench SD). Both join the
   tailnet as `rpi4-bench` (MACH_BENCH_SSH), so the second card's write is
   refused while the first's node exists. Per-role names (`rpi4-rescue` and
   `rpi4-bench`) would avoid it; say which.

Do that.

Checkpoint (agent): `./wk selftest --quick` on the tree after round 1:
501 tests, OK, 22 skipped (was 422/1 failure -- `wk push status --all`
leaked ssh's error for a down machine and called it "no wk-tools there";
now `machine_answers` in lib/target.sh tells unreachable from unprovisioned
for push/sudo alike). Also fixed: `wk status` printed this machine's block
once per job feeding it (twice on macOS: container + vm) -- a block now
prints once, when its last job has flushed (tests/test_status_streaming.py).

Answered, and what was done:

1. (rpi3-bench/rpi4-bench/rpi5-bench) done: MACH_BENCH_SSH is the name a
   system wk writes joins as (`<board>-bench`); rpi5's own install keeps
   `rpi5`; rpi3/rpi4 are reached by the bench name in both roles
   (boot/machines/*.conf, cmd/sysimage _tailnet_name_for, boot/machines.sh
   image_addr, tests/test_tailnet_name.py). The cards wait on A.
2. (WiFi from the running machine) done: admin/wk-card-priv `wifi-host`
   (read-only probe, SSID only) and `wifi-from-host <dev>` read the card
   machine's own netplan credential as root and seed it; the hand-made file,
   WK_WIFI_CREDS and the doctor entry are gone (tests/test_wifi_seed.py; the
   parser ran for real in the podman VM). Found on the way: rpi5's sudo is no
   longer passwordless, so image/pmos-build.sh's `sudo -n cat /etc/netplan`
   cannot run there (docs/HANDOFF-fleet.md).
3. (2.52, not 2.54) done: the 2.54 conf is deleted. The rpi5 image on
   moose is 2.54 and needs rebuilding as webkit-2.52-yocto-rpi5-64 -- waits on A.
4. done (nothing to change).
6. (yocto incremental) the fix is structural, not one-time: upstream
   cross-toolchain-helper's build_image() returns early when
   build/image/<recipe>.<ext> exists (verified in WebKit main, line 488);
   image/yocto-build.sh deletes that copy before every image stage and then
   requires the newest artifact to postdate the stage's start
   (clear_stale_image_copies, verify_image_freshness; tests/test_yocto_stage.py).
   bitbake's own incrementality (sstate) is untouched. A timed second build
   on moose is the remaining evidence -- waits on A.
7. (distro perf image) scrubbed: HANDOFF-bench-unify item and the
   HUMAN-benchmark-start line; `perf-linux-*` stays only as a tombstone
   naming the yocto profile.
8. (image store) done: no mention outside cmd/sysimage's tombstones;
   tests/test_image_store_gone.py polices definitions, calls and the phrase.
   The audit found no other cached fact; two stale labels (lib/store.sh,
   cmd/disk) fixed.
9. (pre-field images) the `unrecorded` word is gone (cmd/find says what is
   missing and names the remedy); deleting the images on moose waits on A.
10. (--sysroot) the doc is rewritten from your notes as owed work
    (docs/Nice to have/HANDOFF-cross-compile.md); still Nice to have.
11. (reprovision) order written into docs/HANDOFF-reprovision.md: rpi5 by
    hand first, then moose, then tolken, each gated on wk doctor and a clean
    wk backup diff. The settings audit itself is docs/HANDOFF-settings-audit.md.
12. (Tailscale ACL) yours, at the end.
13. (--browser) done in code: `wk profile --browser` on GTK/WPE profiles the
    web process by default (samply attaches by name), `--process
    ui|web|network|gpu` for the rest; every combination is dry-run tested,
    none has run on a board yet (docs/Urgent/HANDOFF-profile.md).
14. (Mac sticky startup disk) kept as is.
15. (software-update denial in /etc/hosts) done in code: one function
    (bench/mac-quiet-hosts.sh) applied by first boot and by --provision,
    reported by wk quiesce status; verification on the real bench volume and
    the deep-quiescing programme are owed in docs/HANDOFF-mac-perf-mode.md.
16. (ddebs) done: ddebs.ubuntu.com allowlisted (the VM's proxy restarted on
    the policy change) and firstrun provisions the ddebs apt source; existing
    workspaces need `wk rm`/`wk new` to get it.

Everything else below proceeds without you.

Defects:

- Audit what tools are needed on the host macOS and Linux systems; keep the
  host minimal and use containers / VMs where reasonable. (agent: the
  inventory is done -- every host tool classified; what must stay is the
  runtime (podman, tart+softnet), the proxy/broker pythons, tailscale, the
  display/GPU stack of a bench box, the card machine's media tools and the
  developer's identities (gh, zed). Dropped now: host ccache (the workspace
  image has its own). The decisions and the rest are docs/HANDOFF-host-tools.md.)

- Confirm new recovery images for rpi3 and rpi4 build and write fine. Both sd card (rpi3) and second usb stick (rpi4) are connected to rpi5, so no intervention is needed. I give you permission to do this.
  (agent: both images are built for real on moose -- see decision 1. The
  writes wait on decisions 1 and 2. The first-ever buildroot image,
  wpewebkit-2.38 rpi3, finished on moose at 20:08: sdcard.img 1.6 GB,
  `stage 'image' done` after the freshness check. `wk sysimage ls` prints `?`
  in its machine column -- the profile is not derived from a buildroot
  workspace path; owed with the image-store removal.)

- Confirm rpi5 2.54 yocto image works and can be deployed.
  (agent: built for real on moose, 19:27; the write waits on decisions 1-3)

- wk status should load local data quickly first and fill the rest later.
  (agent: the streaming render is in; a bare `wk status` also hung forever --
  a job returning non-zero left no `.rc` marker -- fixed in lib/par.sh,
  tests/test_par.py; 10.5s end to end now)

- Audit every subcommand for option consistency: a missing --quiet, --target,
  --all or --force where a sibling command has one; a parameter silently
  ignored instead of refused; one section of a command getting a feature a
  neighbouring section does not. Known leftovers from the 2026-08-26 pass:
  --quiet is parsed locally by ~11 commands instead of once in the
  dispatcher, the way --force is; wk ls and wk find have no --json, though
  wk status does; --all means "every machine" in wk sync and "every item"
  elsewhere -- one meaning, or two words; --count appears in bench, pick, pr,
  pi, status and sync with no confirmed single semantics.
  (agent: measured -- only cmd/sudo parses a wk --quiet; the other hits are
  git/systemctl flags. --count is a wk flag only in bench and pi; the rest are
  `git rev-list --count`. Being moved into the dispatcher / audited.)
  (agent: done -- --quiet is stripped once by the dispatcher into WK_QUIET and
  honoured by log/info in lib/common.sh; `wk ls --json`, `wk find --json`;
  one meaning of --all documented in `wk help`; unknown flags refused in
  sudo/ls/find/sync/push/doctor/remote/logs/verify; tests/test_options.py.
  Second pass: status, bench, pr, gc, quiesce, test, gui, profile refuse
  unknown flags and extra positionals with `usage:`; cmd/gc parsed only $1
  before. run/enter/claude pass through by documented contract. --count is
  "iterations per run" in bench and pi. Owed: none.)

- High-level docs: an architecture overview and every supported workflow by
  example, very short. README.md now has a first version of this; the item
  stays until it is accepted as done.

- Mass deletion of low-level comments and docs that serve no value. Comment
  density is still ~37% of shell lines; lib/target.sh is 43%.
  (agent: 42% -> 32% of shell lines across the tree, code verified unchanged
  file by file against HEAD; lib/target.sh 51% -> 37%, cmd/status 27%,
  cmd/bench 33%, cmd/* else 27-34%, targets/ 26-30%, bench/ 25-29%, lib/
  <30%, image/boot 27-30%. Still above 40%, all provisioning/board scripts
  one agent judged load-bearing: remote/provision.sh, vm/provision-base.sh,
  container/firstrun.sh, shell/bashrc, image/buildroot-build.sh,
  boot/mac-guest.sh, host/dotfiles.sh, host/macos/sharing.sh, bridge/bin/*,
  the two yocto join scripts. A second opinion on those is owed.)

- docs/Urgent/HUMAN-benchmark-start.md and docs/Urgent/HUMAN-Benchmarking variance.md
  (agent: done so far -- one result schema with a configuration tuple, `wk
  bench report` HTML/histograms/score+time/variance-by-configuration, the
  noise knobs (ASLR, env pad, path pad, shared cache) in every lane; the
  image/bench-mode unification is planned in docs/HANDOFF-bench-unify.md.
  Still owed from the notes: Mac bench-mode checks in `wk doctor` (screen
  state in quiesce, SIP on host / off on bench volume, single manual step),
  the 8-hour autonomous run driver, one run per side under samply with
  jitdump, and the VM rehearsal of the whole flow.)

- Then go through all the docs not in a folder
  (agent: every HANDOFF-*.md touched by landed work lists owed items only
  now -- wk-cli, test-runner, fleet, pi-deploy, memory, debug, helix, clangd,
  yocto, boot -- and the narrating lines ("no longer", "now that", "used
  to") are rewritten present-tense. Not yet re-read end to end: bmc, ab-bench,
  sdcard, linux-pi, linux-arm32, router, reprovision, sandboxing,
  settings-audit, architecture-review, original-helpers, bench-python,
  bench-unify, mac-perf-mode, Security/tailscale, Nice to have/*.)
