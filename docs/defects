Everything is answered inline.

Focus on macOS development first, then on the other defects.

Use subagents wherever possible to reduce cost and preserve your context window.

Remember the goal: everything should be reproducable. It should be easy to reset the dev environment to a known good state. Each workflow should be tested, because we keep getting bugs that are fixed on one workflow/driver reproduce on another. Generalize fully each defect. Prefer good architure that makes bugs impossible over fiddly hacks, and use 3rd-party tools / libraries whenever you can. Whenever it is possible to upstream something, call it out.

================================================================================
NEEDS YOU
================================================================================

1. A tart guest is a second holder of the claude.ai login. The Claude CLI
   rotates the refresh token on every refresh (measured in the CLI: the new
   refreshToken replaces the stored one under a compare-and-swap, and a
   superseded one is answered invalid_grant and blanked). Every container on
   a machine therefore shares ONE writable file (~/.config/wk/agent-rw,
   mounted rw) and the CLI's own .storage-write lock serialises them. A
   guest mounts nothing of the host, so `wk vm start` writes it a COPY and
   warns: a refresh in that guest invalidates the file every container
   shares, and vice versa. Shapes: (a) accept it -- use claude in a guest or
   in containers, not both at once; (b) pass the agent-rw directory into the
   guest with `tart run --dir` (the guest then mounts one host directory,
   which targets/vm.sh today forbids by design); (c) a per-guest login
   (`claude auth login` inside the guest, no copy). [decision]

   do c then

2. With push on, the api.github.com injector authenticates whatever the
   workspace sends (Host is forced to api.github.com, Authorization is
   replaced, one request per connection), so a workspace can use the whole
   API as your account, not only open a pull request. Restricting method or
   path (e.g. GET anywhere, POST only to /repos/*/pulls) is a policy table in
   container/proxy/github-inject.py. Restrict, or leave whole-API while push
   is on? [decision]

   restrict it to only things that git-webkit needs

3. buildbox4 has no libc++ (measured 2026-09-03: clang++ and clang++-19 are
   the same Debian clang 19.1.7 binary; `-stdlib=libc++` cannot find
   <string>; dpkg lists libstdc++-14-dev only). The libc++ flags are the
   default for every Linux clang config and `WK_TARGET_LIBCXX=0` in
   targets/hosts/buildbox4.conf turns them off there, with that reason. Your
   reference command uses them, so it was run somewhere else or before the
   package went away. `sudo apt-get install -y libc++-19-dev libc++abi-19-dev`
   there, then delete the line. Also measured there: gtk-release took 25m42s
   (build.status in your image-decoders workspace, -j64, no ccache), not
   3m00s and not 40m. [decision]

   As long as a fresh build completes this is fine.

================================================================================
OWED, WITH THE DECISION ALREADY MADE
================================================================================

- The podman machine recreate (yours: the classifier refuses it from an
  agent). `./setup --stage machine --dry-run` reports what goes: the test
  leftover wk-test-isvvgw, 14G mirror, 21G base snapshots, 26G cache, an
  empty bench. Before destroying, the current machine holds the only copy of
  the two deploy keys; either re-register them (`wk key register`, then add
  the new public keys to the forks on GitHub) or carry them across by hand
  first:
      podman machine ssh wk -- tar -C /var/lib/wk -cf - secrets \
        | tar -C ~/.config/wk -xf -      # then move build_key_* (private
                                          # halves) into ~/.config/wk/push-keys
  Then:
      claude auth login          # if not already
      wk key set claude-login
      wk key set github-pat
      WK_YES=1 ./setup           # destroys and recreates wk with three mounts
      wk sync
      env -u WK_NAME -u WK_TARGET -u WK_TARGET_KIND python3 -m unittest \
        tests.test_machine_mounts tests.test_push_agent tests.test_build
  The two unverified assumptions those tests settle: applehv honours `:ro`
  on a `--volume`, and a container's own uid can write+rename inside the
  agent-rw virtiofs mount. If the second fails, `wk_agent_rw_dir` gets a
  VM-local answer plus a seed step; nothing downstream changes.

Done. One issue:

==> stage: vmtools
error: /opt/wk-tools/wk is not executable inside 'wk', so nothing in
    there can run this tooling. The machine mounts this checkout there when it
    is created:  ./setup --stage machine
warning: stage 'vmtools' failed (exit 1) -- continuing with the remaining stages

tolken:host ~/Development/wk-tools main* ❯ wk sync
bash: line 1: /opt/wk-tools/wk: No such file or directory

- Live check after the recreate: `wk new <ws>`; `wk push on`;
  `podman machine ssh wk -- 'SSH_AUTH_SOCK=$XDG_RUNTIME_DIR/wk/ssh-agent.sock ssh-add -l'`;
  in the workspace `git push --dry-run fork HEAD` and
  `curl -sS -o /dev/null -w '%{http_code}\n' https://api.github.com/user`
  (200); `wk verify <ws>`; `wk push off`; the curl again (401);
  `wk verify <ws>`; `wk ai claude <ws> --rc` starts and survives 60s.
  A workspace created before this change keeps its old ~/.ssh/config; one
  `wk rm`/`wk new` converges it.

- `wk vm base --rebuild` (hours). The failure at its end is fixed
  (config_load now gets the platform; a prebuild failure can no longer unmake
  a provisioned base; the password change is verified by `dscl -authonly`,
  not by sysadminctl's exit status). Owed live on a real guest: the rebuild
  itself, the guest mirror's first fetch (targets/vm.sh puts a bare mirror
  carrying all four remotes in the base; a workspace is a --shared clone of
  it), the password actually becoming `1`, and the gitconfig include that
  provisioning now installs.

================================================================================
STILL OPEN
================================================================================

- The `remote` target has no ssh-agent, so `wk push` on a build box reports
  "always live, no switch on this machine" (remote/provision.sh points
  core.sshCommand at the private half). Installing wk-ssh-agent.service there
  through host/units.sh is the owed work (docs/HANDOFF-sandboxing.md).

- devbox-arm64-2's mirror update fails with `could not read Username for
  'https://github.com'`. Retry when GitHub is healthy; if it persists, the
  box has lost its route or credential helper.

- BASE still reads `?` for a workspace on buildbox4 until
  `wk sync --tools buildbox4` puts this tree's commit there (needs a
  committed tree, which you now have).

- Confirm layout tests can run headless and with a real GPU.

- Confirm ccache still caches across a change of -j (an argument, not a
  measurement: nothing wk passes the compiler varies with the job count).

- Live measurement owed on a real tart guest: `wk vm check <name>`'s resident
  shells / editor server report, and which Software Update key a tahoe guest
  obeys (TODO in vm/desktop.sh).

- Guest egress from inside a guest is unmeasured (TODO in cmd/verify); the
  credential and GPU checks run there now.

- The comment pass: dead functions, dated narration and the two design-laden
  handoffs are gone, but shell comment density is still ~35% across the
  densest 25 files (boot/pi-tryboot.sh 54%, container/firstrun.sh 43%,
  cmd/sync 40%). Most of what remains is failure-mode and contract prose;
  say whether that density is acceptable or name the ceiling.
