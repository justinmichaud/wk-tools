- wk enter does not work across machines

- No way to easily close workspaces

- wk rm shoud take multiple hosts

- bash autocomplete

- Why is wk sync so slow on rpi5?

- wk status missing load data for other machines

- wk status should load local data quickly first and fill the rest later

- rpi3 and 4 rescue images don't have tailscale

- Extract a command to find devices via ssh + nmap on local network

- wk status should explain how to generate an image for each member of the fleet (to re-provision it from scratch) and have a small sample command for how to provision each unique role.

- Too many if branches. Everywhere behaviour can change must be tested and
  testable, otherwise the branch does not stay. Needs a starting point: which
  file or command to take first. `cmd/status` (1200 lines) and
  `targets/container.sh` (39 KB) are the two with the most.

- Docs need to be more concise and higher-level. README.md is 24 KB and
  SETUP.md is 33 KB; docs/TESTING.md is 1000 line items. The `docs/help/` pages
  are the concise layer and the two top-level files largely restate them.

- Conf files need to be more consistent; no stupid comments. The twenty new
  `image/configs/*.conf` are the first set written to one shape; `boot/machines/`,
  `targets/hosts/` and `bridge/hosts/` predate it and do not match each other.

- We need to re-provision rpi3 and 4 from scratch and confirm they work

- Get a wpe-2.38 build working and automated with helpers (tailscale + anything else needed to verify the image) for rpi3. Make a buildroot image for rpi4 2.38 (32-bit) and rpi5 (64-bit) too once that is working. 

- Confirm rpi5 yocto image works

- Ctrl-C does not always work as expected. Not investigated.
