Everything is answered inline.

Focus on macOS development first, then on the other defects.

Use subagents wherever possible to reduce cost and preserve your context window.

Remember the goal: everything should be reproducable. It should be easy to reset the dev environment to a known good state. Each workflow should be tested, because we keep getting bugs that are fixed on one workflow/driver reproduce on another. Generalize fully each defect. Prefer good architure that makes bugs impossible over fiddly hacks, and use 3rd-party tools / libraries whenever you can. Whenever it is possible to upstream something, call it out.

================================================================================
NEEDS YOU
================================================================================

1. Claude remote control cannot run on the credential every workspace gets.
   Measured live in a fresh container (`wk new wk-livecheck`, 2026-09-02):
   `claude remote-control` exits at once with "Remote Control requires a
   full-scope login token. Long-lived tokens (from `claude setup-token` or
   CLAUDE_CODE_OAUTH_TOKEN) are limited to inference-only for security reasons.
   Run `claude auth login`". So the one-token-per-machine design (`wk key set
   claude`) and remote-control-on-by-default (`wk new`, `wk start`) contradict
   each other, and every `wk new` has been printing a warning. Three shapes:
   (a) turn remote control off by default and keep the token -- one line,
   `wk ai claude <ws> --rc` stays for a workspace where you ran `claude auth
   login` once by hand; (b) deliver a full-scope login (the ~/.claude
   credentials file `claude auth login` writes, Linux containers only -- it is
   the shared credentials file removed this run) beside the token, for remote
   control alone; (c) both. (a) is honest today; (b) widens what an agent holds
   to a credential that can post as you. `wk ai claude --rc` now reports the
   refusal with the log's last line instead of "running". [decision]

Do B. Agents should be able to interact with anthropic just fine.

   

2. Containers hold no GitHub credential, by design: `wk pr` runs on the host,
   the egress proxy refuses api.github.com and uploads.github.com, and the
   sandbox's premise is that nothing in a workspace can post as you. You asked
   for the WebKit PR credentials to be copied in. That is the one credential the
   sandbox exists to withhold: with it, an agent's `gh pr create` or `git-webkit
   pr` publishes under your name. Two honest shapes: (a) keep `wk pr` on the host
   and add whatever step is actually missing in the workspace -- name the step
   you hit; or (b) a scoped, read-only token delivered like the deploy keys
   (present while push is on, withdrawn while an agent runs), with the proxy
   allowing api.github.com for GET only -- days of work, and it widens the
   sandbox. Nothing is done until you choose. [decision]

Inside a container, I should be able to push and do git-webkit pr. This should be able to be toggled off, and it should be disabled automatically whenever claude runs. I should be able to run wk ai claude/pi inside the container, and it should verify this fact.

The credentials must be moved outside the container, otherwise the agent can still find them.

The proxy should be widened, we can focus just on the credentials.

This should work on macOS vms too.
   

3. `wk push on|off` starts the podman machine even when the work is entirely
   about a tart guest, and there is no honest way around it today: measured with
   a stub `podman` on PATH, `on` and `off` reach `podman machine start` (the
   dispatcher's one forward) and `status` does not (its readonly declaration).
   `off` would not need the store at all -- removing a guest's copy is a `rm` in
   the guest -- but `on` cannot be done without it, because the key bytes exist
   nowhere else on this machine. So the start is now stated rather than
   silent ("the deploy keys live in the podman machine's store, so this reaches
   into it").

   The one real fix: make the macOS host's own secrets directory the store the
   podman machine *mounts*, instead of keys living inside the machine's disk.
   Then `wk push`, `wk key set` and every guest's key and token need no running
   VM at all, and the three `podman machine ssh` hops in lib/store.sh go.
   Assessment of cost: it is a `./setup`-time change to how the machine is
   created (`podman machine init --volume`), so it lands only on a machine that
   is recreated -- which is the "no in-place upgrades" rule working as intended,
   but it means one deliberate recreate of the `wk` machine, and the secrets in
   the current machine's /var/lib/wk have to be re-registered (`wk key
   register`, `wk key set claude`) rather than migrated, since nothing here
   copies a credential between stores. Everything else about the store stays
   where it is. Say the word and it goes in `./setup`. [decision]

   This should go in setup, and setup should tear down any secrets already on the machine or from a previous version of wk. Not in-place: destroy and re-create.

4. buildbox4 has no libc++, so the `-stdlib=libc++` flags you asked for cannot
   go in. Measured: `clang++ -stdlib=libc++` there cannot find <string>, and
   `gtk-release` builds fine against the system libstdc++ (it did, in 3m00s,
   while this was being written). The generic half of your flag list is now the
   default for every CMake config (build/configs.sh, _CFG_DEFAULT_ARGS /
   _CFG_DEFAULT_CMAKE); the libc++ half is not applied anywhere, and
   targets/hosts/buildbox4.conf records why.

   To have it: `sudo apt-get install -y libc++-19-dev libc++abi-19-dev` on
   buildbox4 (theirs to run, or ask), then say so and it goes in the conf.
   Or was that flag list copied from a different machine? [decision]

   This was already decided on, clang was using gcc's libc. gtk-release takes 40min to build, so I question your results.

5. The new default flags are verified as far as `cmake` -- the whole set
   configures cleanly on buildbox4 for GTK with clang 19, in a scratch build
   directory that was then removed. A compile-and-link verification is owed and
   was not started: buildbox4's load average is 142, `wk build` derives 1 job
   from that, and `-DUSE_LIBBACKTRACE=OFF` (previously ON there) invalidates
   the whole tree -- a full rebuild at -j1 in your workspace. Say when the box
   is quiet, or when to just do it anyway. [decision]

Keep the flags. This command is known-working: CMAKE_BUILD_PARALLEL_LEVEL=40 CC=clang-19 CXX=clang++-19 nice ./Tools/Scripts/build-webkit --release --gtk --asan --export-compile-commands --cmakeargs="-DCMAKE_BUILD_TYPE=RelWithDebInfo -DUSE_LIBBACKTRACE=OFF -DDEVELOPER_MODE=ON -DUSE_VULKAN=OFF -DENABLE_THUNDER=OFF -DCMAKE_CXX_FLAGS=-stdlib=libc++ -DCMAKE_EXE_LINKER_FLAGS=-stdlib=libc++ -DCMAKE_SHARED_LINKER_FLAGS=-stdlib=libc++ -DCMAKE_MODULE_LINKER_FLAGS=-stdlib=libc++ " --no-fatal-warnings --makeargs="-j40"

6. "Never rsync wk tools, always use git" now holds for the two machines reached
   over ssh: a build box ($ROOT/tools) and a tart guest (~/wk-tools) each carry
   a git checkout at this tree's HEAD (lib/tools.sh, on every `wk vm start`, `wk
   sync --tools`, `wk remote setup`, and before `wk build`/`wk test` on a vm
   target), and the push refuses while this tree is dirty. One consequence: `wk
   build <guest>` on a vm target refuses while wk-tools has uncommitted edits,
   naming the commit as the remedy. The third copy, host/macos/vmtools.sh ->
   the podman VM (/opt/wk-tools), is still rsync: it is what every container
   workspace mounts and what `wk pr`, `wk build` and friends run when they
   forward into the VM, so refusing a dirty tree there means no container
   command can be tested against an uncommitted wk-tools edit at all -- and an
   agent cannot commit. Does the rule cover the podman VM too? [decision]

The previous decision invalidates this.

7. moose's ~/Development/wk-tools has uncommitted edits
   (bench/mac-ab-summary.sh, cmd/bench, cmd/pi, cmd/status, lib/bench.sh,
   lib/common.sh, lib/wkdata.py) that are not in this tree. Nothing here has
   touched them and `wk sync --tools` has not been run. Yours to commit or
   discard. [decision]

   Done

8. `MACH_` is being renamed (it collides with macOS's own "mach"). Proposed
   `NODE_`: boot/machines/<name>.conf becomes NODE_SSH, NODE_DEVICE, NODE_LOCAL,
   and machine_load/machine_list keep their names. It is one sed over ~700
   occurrences in 35 files -- and it is being held for two reasons, not one: the
   word is yours to pick, and a rename that size would collide with moose's
   uncommitted edits to cmd/status, cmd/pi and lib/common.sh (item 6). Resolve 6
   first, then say the word. [decision]

   Do it now.

9. The tart guest password becomes `1` as asked, which means provisioning
   actually changes the account password (the Cirrus images ship admin/admin
   and `sysadminctl` needs the current one). Confirm the guest account is
   throwaway in the sense that matters: nothing on the tailnet trusts it, and
   `wk rm` destroys it. [decision -- proceeding on yes unless you say otherwise]

   Yes and record this fact in the documentation (not in handoffs). Every singe machine and workspace is disposable in this sense, except remote machines (not managed by me), and workspaces with uncommitted/unpushed changes.

10. jsc-debug lost `-DUSE_LIBBACKTRACE=ON` in commit 027607f, which was not
   made from here, while its own one-line description still said
   "libbacktrace" -- so the config and the listing disagreed. The listing now
   matches the code (jsc-debug: "JSCOnly, Debug, assertions on"), on the rule
   that the machine wins. gtk-debug still asks for libbacktrace. If jsc-debug
   is meant to have it, putting the flag back is one word; if the removal was
   because the library is not on every machine, then gtk-debug's copy should go
   the same way. Which? [decision]

   libbactrace should be on for every machine we setup, but off for remote targets by default.

11. `wk key tailnet` now refuses an API access token and an OAuth client secret
   by name (both start `tskey-`, both administer the whole tailnet, and this
   key is copied onto every card written from here). What it cannot check from
   a key -- tagged, reusable, not ephemeral, expiry -- it reports as unverified
   with the console link, because answering those needs an API token, which is
   the credential it just refused to hold. Nothing further is checkable without
   holding one. Good enough, or do you want a read-only API token stored for
   it? [decision]

   Good enough.

================================================================================
OWED, WITH THE DECISION ALREADY MADE
================================================================================

- `wk rm br-rescue2` (or `podman rm`) -- the Exited container on this Mac's
  host podman connection. You said to go ahead; not done yet.

- Run `wk vm base --rebuild` (hours): the golden base on this machine predates
  its own provisioning inputs, so every guest cloned from it still carries the
  image's admin password rather than the one wk sets, and the wk-tools a build
  happened to leave in it. `wk vm ls` says so under BASE, `wk new --target vm`
  warns before cloning it, and `wk doctor` reports it -- each naming this same
  rebuild. Nothing in a guest is wrong that the rebuild does not fix.

================================================================================
STILL OPEN
================================================================================

- Measured live on this Mac (2026-09-02): `wk sync --tools` refuses the dirty
  tree for buildbox4 and devbox-arm64-2 (the rule, working); moose refuses the
  pull because of its own uncommitted edits (NEEDS YOU 6); and the mirror
  update on devbox-arm64-2 fails with `could not read Username for
  'https://github.com'` -- that box cannot reach GitHub over https without
  credentials, so its per-machine mirror never refreshes. Not wk's to fix by
  itself: the box needs a route to github.com (a proxy, or a credential
  helper), and until then `wk sync --tools devbox-arm64-2` reports the mirror
  as failed, as it did.

  It should be able to reach github, it has worked in the past. There are ongoing issues with github at the moment.

- Live check owed on a real tart guest: `wk push on`, then a `git push fork` in
  the guest actually reaching GitHub through the ProxyCommand, and `wk push off`
  taking the key back out of it. Everything about it is measured against a fake
  guest (a scratch directory a stub ssh runs the real remote commands against),
  which cannot prove that macOS's `nc -X connect` and wk-proxy agree about
  CONNECT to port 22, or that the guest's own account can read the key.

- BASE still reads `?` for a workspace on buildbox4: that machine's tooling
  predates `ws_upstream_line` and answers the delegated listing itself, so it
  reads correctly only once `wk sync --tools buildbox4` puts this tree's
  commit there -- which needs a committed tree.

- Confirm layout tests can run headless and with a real GPU.

- Confirm ccache still caches across a change of -j. (Nothing in the flags wk
  sets varies with the job count -- CCACHE_* is fixed and -j reaches ninja, not
  the compiler line -- but that is an argument, not a measurement.)

A previous agent made this claim FYI

- `wk sync <ws>` on a build machine still fetches its four remotes over the
  network, though that machine keeps a bare mirror of its own beside the
  workspace (targets/remote.sh, `_remote_mirror_update`). The fetch already
  prefers a local copy when the checkout has one in reach, so this is one
  argument -- the path from `t_wiring_args` -- away; what stops it is that
  the build machine's mirror carries only origin's `main`, so taking it
  would silently stop carrying the forks' branches. Carrying
  wk_mirror_default_remotes into that mirror is the fix, and it costs disk
  on every build box.

It doesn't cost disk space, it saves it, no?

- Live measurement owed on a real tart guest: `wk vm check <name>` now reports
  what is resident in there -- shells, an editor remote server that outlives its
  window, agents, the memory macOS itself says is free, and swap -- against
  thresholds (WK_VM_SHELLS_WARN, WK_VM_MEM_FREE_WARN_PCT, WK_VM_SWAP_WARN_MB).
  Nothing wk runs in a guest can leave a shell behind (t_exec is one `bash -lc`,
  t_exec_tty and t_enter exec an ssh that ends with the connection, and this
  driver's ssh sets no ControlPersist master), so the report names the editor's
  remote server as the thing that keeps them: that has to be confirmed on the
  guest that ran out, not against the captured `ps` sample the arithmetic is
  tested on.

- Live measurement owed on a real tahoe guest: which Software Update key the
  offer actually obeys -- see the TODO in vm/desktop.sh.

- git fetch origin in a tart guest is very slow. Each time I report a bug, it only gets fixed in one place. Check everywhere this time, test everywhere.
