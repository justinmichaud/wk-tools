# HANDOFF — debug provisioning

`wk run --lldb`, `wk gui --lldb`, `wk test --layout --lldb` are built; what's left is unverified.

- [ ] verify a crash inside glibc/Mesa/GTK backtraces with symbols in a freshly created workspace
- [ ] verify `gtk-debug` runs under lldb, and an actual Debug build of a GLib port under a debugger
