# HANDOFF — the unittest suite in `tests/`

`wk selftest` runs the stdlib unittest suite under `tests/`; below is the behaviour that has no test yet, most of it writable with a stub/fake target driver and no hardware.

- [ ] test: one kill-point test class per command for crash-only convergence: `wk build --babysit`, `wk test`, `wk bench` (seed and run), `wk vm base`/`--refresh`, `wk vm start`/`stop`/`start`, `wk remote setup`/`rm`, `wk pi setup`, `wk key register`, `wk skills pull`/`push`, `wk backup`, `./setup`, `wk quiesce on`/`off`, `wk session`, `wk ai claude`
- [ ] test: two `wk build` on one workspace serialize on the workspace lock
- [ ] test: `wk vm base --refresh` is refused while one is already running
- [ ] test: two `wk vm start` do not corrupt `~/.ssh/config.d/wk`
- [ ] test: a remote workspace whose clone is cut mid-way reads `creating` from any machine that asks, not only the driving one
- [ ] test: `wk enter --zed` against a `broken` workspace refuses and names the repair command
- [ ] test: deleting `$WK_STORE/ws/<n>` under a live workspace makes `wk gc` refuse to prune what the survivor still pins
- [ ] test: `git fetch` into a published base snapshot by hand is detected and refused by name (recorded sha no longer matches)
- [ ] test: hand-editing `~/.ssh/config.d/wk` — the next `wk vm start` regenerates only its own block
- [ ] test: a delegated `wk status` from an older wk-tools is flagged by version skew, naming both shas
- [ ] test: `wk status --json`/`--html` output is unaffected by the column-alignment renderer
- [ ] test: the same workspace name on two machines is reported as a conflict
- [ ] test: two workstations reaching one build box disagreeing report both views, named
- [ ] test: an armed machine's transition is shown on its status line and flagged desync when stale
- [ ] test: the fleet-walk exit code aggregates the worst state found anywhere
- [ ] test: every `WK_*` override read with a default is documented where the user meets it and covered by a test, or removed (audit pass)
- [ ] test: `wk run` starts on GTK/WPE (catches: per-port build dir, `LD_LIBRARY_PATH` replaced instead of prepended)
- [ ] test: `wk new` waits for and checks the ready marker (catches: firstrun aborting while creation reports success)
- [ ] test: `.config` in a new workspace is owned by the user (catches: the SDK's systemd mount re-appearing and breaking firstrun)
- [ ] test: `wk enter <ws> <cmd>` runs the command (catches: `exec`-ing a shell function)
- [ ] test: an in-workspace build sizes from the whole machine (catches: the desktop reserve subtracted twice inside a guest the host already sized)
- [ ] test: a bare `wk run` in a macOS guest finds a binary (catches: the container-shaped `jsc-release` default resolving a path an Apple-port guest can't have) [needs the macOS guest]
- [ ] test: a guest reaches nothing with the proxy bypassed (catches: softnet not actually enforcing)
- [ ] test: a macOS guest reaches PyPI through the proxy (catches: a raw unset proxy-address variable resolving to a hardcoded dead IP) [needs the macOS guest]
- [ ] test: a macOS guest's `~/.zprofile` carries the `wk-tools: egress` block (catches: provisioning silently not having run) [needs the macOS guest]
- [ ] test: `mac-release-asan` and `mac-release` resolve to different build dirs (catches: Xcode toggling ASan without changing the path, so two builds share one tree) [needs the macOS guest]
- [ ] test: `jsc-release-asan` in a macOS guest builds an instrumented `jsc` (catches: `ASAN=YES` reaching build-jsc's Makefile passthrough but not `set-webkit-configuration --asan`, so the build is clean and the path says `-asan`) [needs the macOS guest]
- [ ] test: the guest desktop is visible after a reboot (catches: screen saver, display sleep, and screen lock each needing to be disabled — any two isn't enough) [needs the macOS guest]
- [ ] test: `WK_TARGET=vm wk gc` runs at all (catches: `cmd/gc` sourcing a driver without `lib/target.sh`)
- [ ] test: a container workspace's `Host wk-<name>` alias is a ProxyCommand, not a hostname (catches: a fictional `HostName localhost` pointing zed at the host's own filesystem)
- [ ] test: `origin` is `WebKit/WebKit` in every target's checkout (catches: a remote build machine pointing origin at its own shared clone)
- [ ] test: `wk build --cmakeargs` is refused (catches: a hand-written one silently replacing `DEVELOPER_MODE`/`USE_LIBBACKTRACE`/arch flags)
- [ ] test: `claude` is on `$PATH` in a container workspace (catches: firstrun installing to `~/.local/bin` with no rc putting it on the path)
- [ ] test: `wk gc` keeps the newest build per profile on a build host (catches: keeping the newest two overall instead, deleting the only un-imported copy of a different profile)
- [ ] test: a lock outlives the command that took it (catches: a flock inherited by podman's `conmon`, holding a workspace lock for the container's lifetime)
- [ ] test: two machines sharing one ssh destination get separate bench-lane state (catches: keying the state file by host alone, so one machine's finished lane reads as done for another)
- [ ] test: the bench result is collected from where it was written (catches: collecting after leaving bench mode unconditionally — wrong for guest vs. volume)
- [ ] test: a bench phase-order change updates the order list too (catches: the high-water mark compared against a hardcoded sequence)
- [ ] test: a shell script is never read mid-edit while it's executing (catches: bash resuming a running script mid-token after a byte-shifting edit)
- [ ] test: `wk quiesce on` returns when driven over ssh (catches: `caffeinate` backgrounded with stdout/stderr still attached, hanging ssh forever)
- [ ] test: `./setup --stage quiesce` succeeds on macOS (catches: an unguarded Linux block masking `getty@tty2`/`autovt@tty2` failures)
- [ ] test: the update-check state is read from the setting, not `softwareupdate --schedule` (catches: that command reporting "on" while the plist says off)
- [ ] test: a preflight check that cannot pass degrades to unknown, not failure (catches: an always-failing check training people to `--force` past everything)
- [ ] test: the per-user Setup Assistant stays suppressed across reboots (catches: writing the suppression keys once, in a session a fresh boot replaces) [needs the macOS guest]
- [ ] test: the bench install is reached at its own address, not the host's (catches: an ssh alias carrying `HostName` for the *host* install, so MagicDNS resolves to the wrong machine) [needs the Mac bench volume]
- [ ] test: a fresh macOS install has `/usr/bin/python3` before provisioning depends on it (catches: a Python heredoc writer failing silently with no CLT) [needs the macOS guest]
- [ ] test: a fresh macOS install's network credentials are checked, not just ssh reachability (catches: "sshd is on" being indistinguishable from "nothing can route to it") [needs the macOS guest]
- [ ] test: the per-user Setup Assistant is checked as a separate pane from the system one (catches: `.AppleSetupDone` suppressing only the system-level assistant) [needs the macOS guest]
- [ ] test: CLT installs headlessly only after its trigger file exists (catches: `softwareupdate --list` never offering CLT without it) [needs the macOS guest]
- [ ] test: an `--installpackage` LaunchDaemon runs on the boot it was installed on (catches: packages laid down after launchd has already scanned `/Library/LaunchDaemons`) [needs the macOS guest]
- [ ] test: a failsafe does not live inside the thing it protects against (catches: a lockout guard written inside the first-boot script it's meant to catch failing)
- [ ] test: the benchmark preflight notices a modal pane owning the screen (catches: checking "is someone logged in" instead of "is the screen usable") [needs the macOS guest]
- [ ] test: `--force` cannot hide a fatal preflight failure behind a benign one (catches: `--force` being all-or-nothing)
- [ ] test: `screen_blocker` asks what is frontmost, not what is running (catches: matching process command lines that are always running, like `softwareupdated`) [needs the macOS guest]
- [ ] test: "is the screen free" has one definition, not one per caller (catches: a lane's own duplicate app list drifting from the shared check)
- [ ] test: only the machine being measured is running during a measurement (catches: a build guest left running and competing for CPU throughout the run) [needs the macOS guest]
- [ ] test: a lane that launches a detached build waits for *that* build (catches: polling `wk status` immediately after `--detach` and reading the previous build's report)
- [ ] test: `wk boot benchvm` actually starts the guest (catches: `guest` matching neither arming branch in `cmd_arm`, so an unconditional stop ran instead and reported `rc=0`) [needs the macOS guest]
- [ ] test: `wk boot <guest-machine>` works from a stopped guest (catches: the `unreachable` refusal firing on the one machine where arming *is* starting it) [needs the macOS guest]
- [ ] test: a `guest` machine needs no `wk sysimage build` (catches: `cmd_arm` sending it down the store-lookup path and dying)
- [ ] test: a driver declares the libraries it calls (catches: a lazy-sourced call moving a missing-dependency failure from source time to call time, mid-arm)
- [ ] test: a guest boots when driven over ssh, not only from a login shell (catches: resolving a helper binary through `PATH`, which differs between interactive and non-interactive shells) [needs the macOS guest]
- [ ] test: `wk boot benchvm --status` reports a stopped guest as stopped, not absent (catches: a caller passing an unmapped workspace name to a function expecting the mapped one) [needs the macOS guest]
- [ ] test: a `command -v <tool>` probe over non-interactive ssh doesn't report a real tool as missing (catches: non-interactive ssh getting a minimal `PATH` that excludes Homebrew/`~/.local/bin`)
- [ ] test: `wk ls` and `wk status` on a macOS host name every workspace (catches: `wk` sourcing `lib/target.sh` without `lib/store.sh` on the macOS code path specifically)
- [ ] test: `wk bench mac-volume` runs on the Mac itself (catches: the macOS host forwarding it into the podman VM by mistake)
- [ ] test: `--dry-run` is the real path with mutations suppressed, not a second modeled path (catches: a dry run reasoning about a fictional disk state its own earlier steps never made)
- [ ] test: `wk sysimage write` onto a reused disk leaves a mountable filesystem (catches: bmaptool leaving previous-system bytes in unwritten holes that are supposed to be zero)
- [ ] test: `wk sysimage build --detach` leaves `yocto.status` saying what actually happened (catches: only the waiting parent writing the terminal state, so a detached build reads `running` forever)
- [ ] test: `wk bench compare` can install scipy in the workspace that built the thing being compared (catches: `pip3 install --user` alone refused under PEP 668 on a plain Ubuntu workspace)
- [ ] test: an on-board run reaches `wk bench compare` at all (catches: `wk pi bench` printing JSON to stdout and never entering the store `wk bench compare` reads) [needs a Pi card in hand]
- [ ] test: `wk pi bench --ab` compares only rounds where both arms finished (catches: a survivor whose partner crashed biasing the comparison) [needs a Pi card in hand]
