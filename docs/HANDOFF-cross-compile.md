We should support cross-compile targets: https://github.com/justinmichaud/webkit-container-sdk/tree/dev/cross-builds-oc 

You have a workspace set up for cross compilation, and a helper to transfer the binary to the target (eg, rpi5 or a remote machine). We should confirm that debugging and perf testing supports this configuration too.
