Make sure that the claude agent will be unable to push on my behalf ever when inside a container. Also confirm that it won't be able to find a gh api token to post publicly on my behalf.

Furthermore, allow me to completely disable the git commit command inside a container if I want.

Remember that a container must remain powerful enough to run a full perf task. That means talking to privileged helpers. Think deeply about what kind of access a motivated rogue agent could escalate to.
