Add a setup subcommand for all devices for testing wifi and connectivity. Wifi channels, reliability, autosleep, etc.

This should also contain perf fixes and configs for devices that support it. Ex: the rpi5 workstation overclocks

setup should check the status of these fixes, but not run them by default. It should tell you how to run this subcommand.

sudo access should go in the subcommand too.
