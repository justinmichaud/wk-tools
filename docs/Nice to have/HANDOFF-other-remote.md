# HANDOFF — remote targets that are not Linux

## Remaining

- [ ] verify the macOS/BSD branches of `_remote_probe_cmd`/`_remote_probe_parse` against a live remote, not only captured samples (`tests/test_remote_driver.py`) [needs a macOS or BSD remote]
- [ ] support Windows build machines as remote targets [decision]
- [ ] support a bare rented cloud VM as a remote target — provisioned from nothing, no containers or virtualization on it [needs a rented cloud VM]
- [ ] exercise the PII-free perf-testing case on a bare rented VM [needs a rented cloud VM]
