#!/bin/bash

cd $BUILDDIR

echo $BUILDDIR

if [[ "$CONFIG" == "Release" ]]
then
    cd WebKitBuild/JSCOnly/Release/ && time mold -run ninja -k 0 jsc
else
    cd WebKitBuild/JSCOnly/Debug/ && time mold -run ninja -k 0 -j79 jsc
fi
