# macos-guest-base -- the golden macOS guest every vm workspace is a copy-on-write clone of, pulled from the pinned Xcode image and provisioned on this Mac
IMG_BUILDER=guest
IMG_MACHINE=""
IMG_ARCH=arm64
