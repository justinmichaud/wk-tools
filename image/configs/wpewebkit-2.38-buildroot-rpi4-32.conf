# wpewebkit-2.38-buildroot-rpi4-32
#
# WPEWebKit 2.38 on rpi4 (32-bit), built with buildroot: BusyBox init, cog on weston, no service manager.
#
# Sourced by image_profile_load. Only what is true of *this* configuration: the
# board, the width, the tree it is built from and whatever that combination
# needs that the defaults do not give. Nothing here restates a default.

# 2.38 is buildroot-only. There is no yocto configuration at this release and
# there will not be one: the WPE 2.38 spec is the fork's release-pinned `cog`
# defconfig, and Tools/yocto on a 2.38 branch has no target section to build
# from. A yocto image at this release would be a different distribution wearing
# the release's name.
CFG_PROJECT=WPEWebKit
CFG_RELEASE=2.38
CFG_BRANCH=wpe-2.38
CFG_REMOTE=wpe

IMG_BUILDER=buildroot
IMG_MACHINE=rpi4
IMG_ARCH=armhf
IMG_HOSTNAME=raspberrypi4
# --- buildroot ----------------------------------------------------------------
# The tree is a *fork* pinned to WPE releases, not upstream buildroot, and it is
# where the release-pinned `cog` defconfigs live.
BR_TREE_URL=https://github.com/WebPlatformForEmbedded/buildroot.git
BR_TREE_BRANCH=wpe
# A commit on that branch, and it has to be a commit rather than the `2020.02`
# tag that used to be here. That tag is the *buildroot version* the branch is
# based on, not a place the tree can be stood at: the release-pinned cog
# defconfigs were added to the branch afterwards, so checking the tag out throws
# away the very configuration this profile names. Measured -- the defconfig is
# present on `wpe` and Not Found at `2020.02`, and a build pinned to the tag died
# at `No rule to make target`.
#
# A branch is not a pin either, which is why this is neither. Bump it
# deliberately; the fork is somebody else's vendor branch and moves without
# reference to us.
BR_TREE_COMMIT=d8ce43b0e3c4a4739e96a84b4e6b7da8185e133a
BR_DEFCONFIG=raspberrypi4_wpe_2_38_cog_defconfig

# The kernel this image runs, declared rather than built. Measured on the
# board 2026-08-31, by staging one kernel at a time onto the SD with
# everything else held identical (boot/pi-tryboot.sh): a kernel Raspberry Pi
# publish boots this Pi 4 every time, and the one buildroot builds from the
# release-pinned rpi-5.15.y tree never reaches userspace -- no console, no
# diagnostics, not even with the known-good device tree and no display
# overlay, so it is the kernel binary and nothing around it. The built
# config is not short of anything a Pi 4 needs (ARCH_BCM2835, MMC_SDHCI_IPROC,
# PCIE_BRCMSTB, LPAE, HIGHMEM, SMP); what is left is the 2020.02 fork's gcc
# 9.2 building a Pi kernel, and answering *that* needs a serial console at
# the board (docs/HANDOFF-boot.md).
#
# Pinning one is the off-the-shelf component, and the kernel is not what this
# lane measures: both arms of an A/B run the same one. A Debian package is
# the whole input in a single content-pinned artifact -- kernel, modules,
# device trees and overlays together -- rather than a kernel from one place
# and modules from another that could disagree.
BR_KERNEL_DEB_URL="https://archive.raspberrypi.com/debian/pool/main/l/linux/linux-image-6.12.96+rpt-rpi-v7l_6.12.96-1+rpt1_armhf.deb"
BR_KERNEL_DEB_SHA256=bafd89a053b4cf6bc8808b35295638b05c5d9b11f54eb20d63ba5b7b2e7dd9ed
# The module directory inside it, which is also the kernel's own `uname -r`.
BR_KERNEL_RELEASE="6.12.96+rpt-rpi-v7l"

# The tailnet overlay, which is how a buildroot image gets onto the tailnet at
# all: BR2_ROOTFS_OVERLAY, assembled by image/buildroot/tailnet-overlay.sh. The
# tailscale binaries are pinned in one file for all three installers
# (image/yocto/meta-wk-tailnet/.../tailscale-release.inc); the overlay carries no
# credential -- the key arrives with the card.
BR_OVERLAY_TAILSCALE=arm
# A BR2_EXTERNAL tree this repository owns, rather than a patch against somebody
# else's vendor branch. It exists for one reason so far: host-python-2.7's
# bundled 2013-era libffi cannot assemble aarch64/sysv.S, so the build dies at
# `sharedmods` on an arm64 build host. Buildroot ships host-libffi and gives the
# *target* python --with-system-ffi, and simply never joins the two for the host.
BR_EXTERNAL=1
# This defconfig's filesystem output is tar-only -- it builds no rootfs.ext4, so
# the board's own genimage config has nothing to assemble. The image wk writes is
# the one genimage produces.
BR_IMAGE=sdcard.img

# Refused rather than attempted: an hours-long build that fails on a missing
# input should fail in the first second, naming the input.
# No CFG_NEEDS: the defconfig this needs now exists. It is not the fork's --
# there is no release-pinned rpi4 defconfig upstream at any WPE release -- it is
# derived in image/buildroot/external/configs, which is where this field used to
# say it belonged. That README carries the derivation line by line, and the fact
# that it has never been built.
#
# 32-bit because that is what the rpi4 is by default in this fork: both
# raspberrypi4_defconfig and raspberrypi4_wpe_defconfig are BR2_arm, and the
# 64-bit ones say so in their names (raspberrypi4_64_*).
