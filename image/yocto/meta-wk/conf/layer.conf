# Build-time fixes only: a recipe here must install nothing into the image, or the release branch's pinned distribution stops being what the profile names.

BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-wk"
BBFILE_PATTERN_meta-wk = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-wk = "10"

LAYERDEPENDS_meta-wk = "core"
LAYERSERIES_COMPAT_meta-wk = "scarthgap"
