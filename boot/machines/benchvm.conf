# benchvm -- a macOS guest standing in for a benchmark install; tart exists only on the macOS host, so the guest is reached through it (`wk enter`).

NODE_SSH=""
NODE_DRIVER=mac-guest
NODE_DEVICE=""
NODE_ROOT=""
NODE_PROFILE=perf-macos-benchvm
NODE_MAC=""
NODE_BRIDGE=""
NODE_ROLE=bench-device
NODE_OS=macos
NODE_LOCAL=""
NODE_VOLUME=""
NODE_DTB=""
NODE_BENCH_SSH=""
NODE_NET=ethernet
NODE_NOTE="A macOS guest standing in for a benchmark install. Rehearses the path, not the number."
